---

- hosts: master
  become: yes
  gather_facts: no
  tasks:
  - add_host:
      hostname: "{{ workers_suffix ~ '-' ~ item.num }}"
      mac: "{{ item.mac }}"
      groups: workers
    with_items: "{{ workers }}"
    when: add == "true" or add is defined

  roles: 
  #- common-master 
  #- kubernetes-master

- hosts: workers
  become: yes
  gather_facts: no 
  roles:
    - common-workers  
    - kubernetes-workers

...
