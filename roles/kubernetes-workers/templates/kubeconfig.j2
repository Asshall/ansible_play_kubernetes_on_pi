apiVersion: v1
kind: Config
clusters:
- cluster:
    certificate-authority: {{ remote_cert ~ 'ca.pem' }}
    server: {{ api_addr }}
  name: {{ cluster_name }}
contexts:
- context:
    cluster: {{ cluster_name }}
    user: kubelet
  name: kubelet
current-context: kubelet
